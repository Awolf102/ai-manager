# AI-Manager — Remediation Cycle Backlog (triage of the 2026-06-27 audit)

Derived from `docs/audits/2026-06-27-tool-audit.md`. Each **cycle** below is one
brainstorm→spec→plan→TDD unit (the project's standard workflow). Findings are grouped by cohesion
(same file/subsystem) so a cycle is a single coherent change. `#N` references the audit's prioritized
remediation table rows.

**Triage principles:** (1) security + irreversible data-loss first; (2) UX-only Importants fold into the
Phase-2 "Orkestr" overhaul (it rebuilds those surfaces anyway); (3) the replan/escalate/handoff Criticals
sit on code paths that have **never run live against real Claude** — live-verify those paths before/while
fixing them.

---

## Fix-now cycles (must-fix-before-overhaul)

| Cycle | Title | Rolls in (audit #) | Primary files | Depends on | Live-verify first? |
|---|---|---|---|---|---|
| **S2** | Run-result launch hardening | #1 | `server-manager.ts`, `run-manifest.ts`, `manifest-detector.ts`, `RunResultModal.tsx` | — | no |
| **U1** | Destructive-action guardrails | #8, #31 | `AgentConfigPanel.tsx`, `OrgChart.tsx`, `project-store.ts` (deleteAgent), `TeamSpawnModal.tsx`, `RoleDraftModal.tsx` | — | no |
| **P1** | Crash-safe persistence (atomic writes) | #4, + run-store `.tmp` leak (Minor) | `project-store.ts` (saveGraph/openProject), `run-store.ts` | — | no |
| **P2** | Race-safe mutations (serialize graph + memory, thread sessionId) | #5, #14, #16 | `project-store.ts` (updateAgent/applyReflection), `agent-runner.ts`, `nodes.ts` | **P1** (same files) | no |
| **S1** | Autonomy blast-radius hardening | #3, #9, #20 | `agent-runner.ts` (additionalDirectories), `nodes.ts` (mode mapping), `SettingsModal.tsx`, `context-files.ts` (framing) | — | no |
| **S3** | Plugin/skill trust hardening | #2, #19 | `skill-trust.ts`, `skill-discovery.ts`, `agent-runner.ts` | — | no |
| **S4** | Team-bundle import validation | #17, #18, + context-file symlink/size (Minor) | `team-bundle.ts`, `project-store.ts` (importTeam/applySpawnedTeam), `agent-runner.ts` (runHeadless fallback) | — | no |
| **S5** | HITL secret handling truth-up | #10, + abort-path scrub gap (Minor) | `nodes.ts`, `run-state.ts`, `graph.ts`, `HitlModal.tsx` | — | no |
| **P3** | Durable resume actually works | #11, #12 | `run-store.ts` (wire `listResumable`), `orchestrator.ts`, `ipc.ts`, `store.ts`, renderer affordance | — | partial (HITL pause) |
| **HITL+R2** ✅ | HITL resume→synthesis + Handoff persistence (batched) | NEW HITL bug + #23, #27, #25 | `nodes.ts`, `graph.ts`, `orchestrator.ts` | live-verify ✓ | DONE 2026-06-29 (merge `f7ed25f`) |
| **R1** ✅ | Replan/escalate state integrity | #6, #7, #13 | `replan.ts` (mergeReplan) | live-verify ✓ | DONE 2026-06-29 (merge `646783e`) |
| **R3** | Scheduling edge cases | #22, #26, #15 | `nodes.ts` (multi-asker), `workflow-order.ts` (self-dep), `project-store.ts` (staged team writes) | — | partial |

### Recommended order
`S2 → U1 → P1 → P2 → S1 → S3 → S4 → S5 → P3 → [live-verify session] → R1 → R2 → R3`

**Why this order:** S2 is a tiny, self-contained fix that closes a command-execution hole — best first win.
U1 is cheap and stops one keystroke from irreversibly deleting accreted agent memory (high user value, no
engine risk). P1 makes persistence crash-safe (the foundation); P2 makes it race-safe and **must follow P1**
(same files). The remaining security cycles (S1/S3/S4/S5) are independent and can be reordered. P3 restores
crash/HITL-pause recovery. The R-cycles come last and **after a live-verification session**, because their
bugs are only reachable on paths that have never executed live — running them once may reveal more (or change
the right fix) before we touch the code.

---

## Deferred to the Phase-2 "Orkestr" overhaul (UX Importants)

These are real, but they are UI-surface rework the overhaul will redo wholesale — fold them into the overhaul
requirements rather than spending point-fix cycles now (per the *functionality-over-polish* preference). They
should be captured as overhaul acceptance criteria:

- #21 per-agent permission dropdown is a silent no-op during runs → collapse to one permission concept.
- #28 GoalBar "Run" silent-fail + inconsistent error surfacing → one non-blocking toast/notification center.
- #29 no success state / final report hidden in live run → "Run complete" banner + render `run.final`.
- #30 (UX half) HITL Skip mislabeled / no abort while paused / guarantee text → modal redesign.
- #32 flagship features default-off with active-sounding copy → real on/off toggles, enable-on-gesture.
- #33 duplicate "Run" buttons / terminal hides live run / undiscoverable canvas edge semantics / ambiguous top-bar icons → IA + canvas legend + coach-marks.
- #34 cost copy + flat ungrouped Settings with danger control last → grouped Settings (Safety/Cost/Review/Team) + cost hints.
- Most Dimension-4 Minors (legend, success confirmations, disabled-button reasons, restore-defaults, dirty-state guards).

> Note: #9 (Full-auto danger styling) and #8/#31 (destructive confirms) are UX but classified **fix-now**
> because they are security/data-loss, not polish — they're in S1 and U1 above, not deferred.

---

## Not a code cycle

- **#35 Live-verification session — ✅ DONE 2026-06-29** (findings: `docs/audits/2026-06-28-live-verification-findings.md`).
  Ran all four "no" rows against real Claude in `~/live-verify`. Results: **HITL** runs live + works (pause/
  modal/answer/resume/apply) — **NEW bug:** resume→synthesis (resumed worker output not captured → final report
  falsely says "unanswered"). **Peer handoff** runs live + works (engineered ephemeral-consult goal; natural
  goals get sequenced) — **R2 confirmed:** handoff leaves no `handoffs` trace in the record. **Re-plan/escalation**
  **won't fire naturally** (orchestrator plans well + adapts in-task) → **R1 un-gated** (its `mergeReplan` bugs are
  unit-testable without a live repro). **P3 durable resume** full pass (bonus). The "never run live" gap is CLOSED.
- The remaining ~30 Minors fold opportunistically into the nearest cycle above, or batch into one optional
  "papercuts" sweep after the must-fix tiers.

---

## Status

- **2026-06-27** — Triage approved. UX-only Importants (#21, #28, #29, #32–34) **deferred to the Phase-2
  Orkestr overhaul** (recorded as overhaul acceptance criteria in the overhaul-plan memory).
- **2026-06-27** — **Cycle S2 (Run-result launch hardening) ✅ MERGED to main** (merge `1ad5a9b`). `parseStartCommand` tokenizes the launch command into argv + rejects unquoted shell operators / leading `VAR=` / unbalanced quotes; `server-manager` spawns `shell:false` (injection sink closed); modal validates live. 293 tests, opus whole-branch review = ready-to-merge. 3 non-blocking Minors deferred. Audit Critical #1 resolved.
- **2026-06-28** — **Cycle U1 (Destructive-action guardrails) ✅ MERGED to main** (merge `89373f7`). `deleteAgent` soft-deletes the agent folder to `.ai-manager/.trash/<slug>-<ts>/` (recoverable `memory.md`) instead of `fs.rm`; reusable promise-based `ConfirmDialog` gates agent deletion (panel button + canvas keyboard via React Flow `onBeforeDelete`) and role-draft Apply overwrite; edge-delete + team-spawn unchanged. 294 tests, opus whole-branch review = ready-to-merge (+1-line `requestConfirm` hardening applied). Audit #8 (Critical) + #31 (Important) resolved.
- **2026-06-28** — **Cycle P1 (Crash-safe persistence — atomic writes) ✅ MERGED to main** (merge `7a4f50b`). New `atomic-write.ts` (`atomicWrite` temp+rename; `atomicWriteWithBackup` demotes old→`.bak` via rename); `saveGraph` writes `graph.json` atomically with a `.bak`; `openProject` recovers from `.bak` / preserves a corrupt file as `*.corrupt-<ts>` / falls back to empty and never throws; `run-store.put` reuses the helper + `sweepTmpFiles` clears orphan `.tmp`. 303 tests, opus whole-branch review = ready-to-merge. Audit Critical #4 resolved. **Concurrent lost-update race remains → P2.** Next recommended cycle: **P2 (race-safe mutations — serialize graph/memory writes + thread sessionId; #5, #14, #16)** — builds directly on P1; reviewer flagged a P2 follow-up: collapse the `atomicWriteWithBackup` `existsSync` guard into the try/catch to remove its TOCTOU window.
- **2026-06-28** — **Cycle P2 (Race-safe mutations) ✅ MERGED to main** (merge `5dd1fd9`). New `mutex.ts` (`createMutex`/`createKeyedMutex`); `saveGraph` runs inside one mutex so concurrent graph mutations can't lose updates (all mutators funnel through it; no re-entrancy); `memory.md` writes serialize per-agent (`memoryMutex` + lock-free atomic `writeMemoryRaw`, wrapping `writeMemory`/`applyReflection`/`refreshFromTeam`); #16 resolved by #5 (persisted `sessionId` now reliable, documented); P1 `atomicWriteWithBackup` `existsSync` TOCTOU removed. 310 tests, opus whole-branch review = ready-to-merge (verified no deadlock/re-entrancy; +keyed-rejection test + saveGraph live-ref invariant comment). Audit #5 (Critical) + #14 + #16 resolved.
- **2026-06-28** — **Cycle S1 (Autonomy blast-radius hardening) ✅ MERGED to main** (merge `ff2bc73`). New pure `permission-options.ts` (`buildPermissionOptions`) sets the SDK-required `allowDangerouslySkipPermissions:true` for `bypassPermissions` (fixes a likely-broken Full-auto run); `agent-runner.streamAgent` spreads it (single query() site). `SettingsModal` gives Full auto honest danger copy + an acknowledgement gate (reuses U1 `ConfirmDialog`) + `.autonomy-danger` styling. `buildContextBlock` reframes user-attached context files as data-not-instructions (`#20`). 313 tests, opus whole-branch review = ready-to-merge. Audit #3 + #9 + #20 resolved (the **danger-UI/correctness/framing** parts). ⚠️ **RESIDUAL (deferred, documented in the spec):** true filesystem confinement of a Full-auto/`bypassPermissions` run is NOT achievable in-SDK (`additionalDirectories` only widens; bypass skips the permission system). A real jail needs an **OS-level sandbox spike** (the SDK `sandbox` option — seatbelt/bubblewrap, macOS-uncertain) — **NEW BACKLOG ITEM, not yet scheduled.** Next recommended cycle: **S3 (plugin/skill trust hardening — #2/#19)**.

- **2026-06-28** — **Cycles S3 + S4 (batched as the "external-data trust" cycle) ✅ MERGED to main** (merge `4ddb665`). **S3 (#2/#19):** `isTrusted` is now mode-aware — strict default (`trustAnthropicOnly:true`) trusts a plugin only if its OWN `marketplace_entry.author.name` is Anthropic AND its marketplace is a verified anthropics-owned github repo (new `isAnthropicOwnedRepo` replaces the bare `startsWith`, resisting `anthropics-evil/`, non-github, host-spoof vectors); broad mode trusts any member of a verified anthropics-owned repo; the **forgeable install-count threshold is removed from trust entirely** (`skillInstallThreshold` deprecated-inert). New `pluginShipsHooks` + fs-probe in `skill-discovery` **excludes hook-bearing plugins** when `blockPluginHooks:true`; `anthropic-only` fallback returns empty (can't establish per-plugin authorship without the cache). All 4 `discoverSkills` callers wired (agent-runner, ipc, role-drafter, team-spawner). **S4 (#17/#18):** `validateTeamBundle` is now validate-AND-normalize — never throws, whitelists `permissionMode`/`kind`/`model`, clamps `role`(50KB)/`lessons`(200×2KB)/members(200), coerces finite positions; `planTeamImport` forces `acceptEdits`; import is now **preview→confirm→apply** (`importTeamPreview`/`importTeamApply` IPC; apply RE-VALIDATES the payload; renderer `requestConfirm` shows members+role-prose-as-untrusted+warnings before any write); context files reject symlinks + >25MB with reasons (the D3 Minor); the **B2b auto-sync path is covered transitively** (`readTeamBrain`→`validateTeamBundle`, lessons-only — no separate routing). **Security controls (user-requested):** new **Settings→Security section** (relocated autonomy + skills-pack) + a **Full-auto lock** (`lockBypassPermissions`, default off) that clamps any `bypassPermissions`→`acceptEdits` at the single SDK boundary (`buildPermissionOptions`). Defaults safe (remediation holds out-of-box); broader trust + bypass are explicit, warned opt-outs. 329 tests, tsc(node+web)+build clean, opus whole-branch review = ready-to-merge (no Critical/Important; all Minors deferred or polished pre-merge). Audit **#2/#19/#17/#18 + context-file Minor resolved.** Next recommended cycle: **S5 (HITL secret handling truth-up — #10)** — first of the serial `nodes.ts` run-loop cluster.

- **2026-06-28** — **Cycle S5 (HITL secret-handling truth-up) ✅ MERGED to main** (merge `253822a`). Audit **#10 + the abort-scrub Minor resolved.** The app no longer durably stores a user's HITL answer in its OWN records. New pure `redactUserAnswer(text, answer)` (`shared/ask-user.ts`): redacts every verbatim occurrence of the trimmed answer with `[user answer redacted]`, gated on `MIN_REDACT_LEN=6` (short decisions like "yes"/"no" aren't secrets and a literal replace would mangle prose, e.g. "no" in "node"); applied at the SINGLE resume output-capture point in `nodes.ts` (`const out = redactUserAnswer(r.text || '(no output)', answer)`), which covers `tasks[].output`, `steps[].output`, the resume checkpoint, AND History via `toRunRecord`. `graph.ts` abort branch now scrubs `resumeInput` (symmetric with the error path; closes the transient cancelled-checkpoint window). `HitlModal` copy made truthful (answer reaches the agent's session transcript like any prompt; redacted from run history; don't paste true secrets). **DECISION:** the SDK `{sessionId}.jsonl` transcript is NOT deleted (document-only — it's the user's own `~/.claude` data + needed for session resume); the residual is the verbatim-only limit (a paraphrase escapes), disclosed in copy + spec. Off-by-default (`maxUserRequests=0`) byte-for-byte. **Also folded in (user-approved) a pre-existing latent product bug found while verifying:** `addRecent` wrote `recent-projects.json` non-atomically → concurrent writes corrupted it → `getRecentProjects`' `JSON.parse` threw → `openProject` cascaded (broke the test suite ~1/8 + could corrupt a real user's recents). Fix: `atomicWrite` for recents + `getRecentProjects` returns `[]` on corrupt file (P1-family recovery) + both `project-store` test files now use a UNIQUE per-file `userData` dir (was a shared `/tmp/recent-projects.json` racing across parallel test files). 337 tests, tsc(node+web)+build clean, opus whole-branch review + a focused review of the post-review commits = ready-to-merge (no Critical/Important). Next recommended cycle: **P3 (durable resume — #11/#12)** — file-disjoint from `nodes.ts`; OR continue the serial run-loop cluster, but **R1/R2 must wait for the live-verification session** (escalation/replan/handoff paths never run live).

- **2026-06-28** — **Cycle P3 (Durable resume actually works) ✅ MERGED to main** (merge `43c4b6b`). Audit **#11 + #12 resolved.** Wires the already-paid-for crash/pause checkpointing (a checkpoint per node) — which was inert (`listResumable` had zero callers, checkpoints leaked, no UI). **No engine resume-logic change** — that path already worked. `run-store.ts`: `gcCheckpoints(nowMs)` (removes terminal-status any age + `running`/`interrupted` older than 30 days by `updatedAt`; leaves unparseable; NaN `updatedAt`→kept) + pure `toResumableSummaries(states, activeIds)` (maps to `ResumableRun` summaries, **excludes the in-memory `active` map** so a genuinely in-flight run is never shown resumable). `orchestrator.ts`: `listResumable`/`discardRun`/best-effort `gcCheckpoints` wrappers; **`resumeRun` answer made optional** across IPC/preload/types (no-answer ⇒ crash-recovery; an `interrupted` run resumed with no answer re-shows its question — the #12 reload-recovery); `openProject` + `pickProjectFolder` run GC after opening. Renderer: `resumable` state + 4 actions; **dismissible banner + History-button badge + History "Resumable" section** (Resume / Discard-with-confirm); refresh-on-open (resets the dismissed banner only on actual project switch) + refresh-on-History-open. Off/empty (no checkpoints) byte-for-byte. 340 tests, tsc(node+web)+build clean, opus whole-branch review = ready-to-merge (no Critical/Important; 4 Minors polished pre-merge). Next recommended: the **live-verification session** (#35), then **R1 → R2 → R3** (serial `nodes.ts` cluster; R1/R2 need the live-verify first). Remaining after that: the **OS-level-sandbox spike**.

- **2026-06-29** — **Cycle HITL+R2 (HITL resume→synthesis + Handoff persistence, batched) ✅ MERGED to main** (merge `f7ed25f`). First item of the serial `nodes.ts` run-loop cluster, batching the **NEW HITL resume→synthesis bug** (from the live-verify session) with **R2 (#23/#27/#25)**. **HITL:** synthesis was blind to `state.userRequests`, so a HITL run's final report falsely reported answered questions as unanswered / mislabeled the applied answer as a placeholder. Fix: new pure exported `formatUserRequests(state)` (built ONLY from the persisted questions — S5-safe, never the answer) appended to `synthNode`'s synthesis input, with the "treat as resolved" directive living INSIDE the section so `synthPrompt` is untouched and off-path is byte-for-byte. A **faithful end-to-end repro** (graph→ask→pause→resume→synth) **passed first-try** → confirmed the live symptom was synthesis-blindness, NOT a capture gap (the resume block's `tasks[].output` capture already worked; `nodes.ts` runtime unchanged for HITL). **R2:** handoffs were emitted as events but never written to state (no durable trace). Fix: a cumulative `eng.handoffs` collector pushed at the single emit site in `runWithHandoffs`, merged into run state via a NEW **generic `NodeIO.collectExtras?: () => Partial<RunState>` seam** in the graph runtime (`runGraph` + folded into the 3 in-node `io.checkpoint` calls for full checkpoint-safety) + orchestrator wiring (init `handoffs:[]`, set `io.collectExtras`, **seed from the checkpoint on resume** — no double-count/loss). `toRunRecord` + the existing HistoryView already surface it (**no renderer change**). Consulted **peer now emits `working/done/error` status** so its run-tree pill reflects activity. **Mechanism refinement vs spec §2c:** chose the single generic `collectExtras` seam over threading `handoffs` through ~13 node return patches (incl. fiddly flat-team early returns) — foolproof + checkpoint-safe, `graph.ts` stays generic. Off-path (`maxUserRequests=0` & `maxHandoffs=0`) byte-for-byte. 352 tests (was 340; +12: `formatUserRequests` units, faithful HITL repro, graph `collectExtras` on/off, handoff persistence worker+review site, off-path no-key, peer status, unknown-asker guard, **combined ask+handoff** regression — the last genuinely fires both a handoff AND an ask in one run, asserting the handoff persists across pause→resume with no double-count/loss and no answer leak), tsc(node+web)+build clean, opus whole-branch review = ready-to-merge (no Critical/Important; 2 Minors — unknown-asker guard + combined test — applied pre-merge). Audit **NEW HITL bug + #23/#27/#25 resolved.** Next: **R1 (replan/escalate integrity — un-gated, code+unit-tests on `mergeReplan`; #6/#7/#13)**, then **R3 (#22/#26/#15)**. Remaining after the cluster: the **OS-level-sandbox spike** (S1 residual) + the ~30-Minor papercuts sweep.

- **2026-06-29** — **Cycle R1 (Replan/escalate state integrity) ✅ MERGED to main** (merge `646783e`). Audit **#6/#7/#13 resolved.** Hardened the pure `mergeReplan` (`src/shared/replan.ts`) against id collisions: when an orchestrator re-plan/escalation decision reuses an existing task's id it previously **clobbered** the frozen/passed task, produced a **duplicate plan id** (plan↔tasks-map disagreement), and could leave a **dangling `dependsOn`**. Fix (chosen policy = **re-id, preserve both** over frozen-wins-drop): a deterministic `freshId(base, used)` (`base~2`, `base~3`, … — no randomness) re-ids any decision task colliding with a FROZEN id or an earlier decision task; deps are remapped to the re-ided ids; dangling deps are dropped on **new AND frozen** tasks; the plan is rebuilt from the now-unique ids so `plan` ids === `Object.keys(tasks)` 1:1. Key correctness detail: `used` seeds from **frozen ids only** — a decision reusing a *replaced* id (legit replacement) keeps its id. **Off-path byte-for-byte** (`maxReplans=0` → `mergeReplan` unreachable); non-colliding decisions byte-for-byte identical **incl. frozen object identity + omitted empty `dependsOn`** (the 8 pre-existing tests unmodified + green). +7 integrity tests (collision proactive + escalation/passed, internal-dup ids, dangling new/frozen, re-id deps remap, a shared `assertConsistent` invariant). 359 tests, tsc(node+web)+build clean, opus review = ready-to-merge (no Critical/Important). **Noted for a future papercuts sweep (pre-existing, unreachable in production):** `frozenInOrder = plan.filter(p => frozen[p.id])` would drop from the output plan a frozen task whose id isn't in the input plan array — not reachable because every `tasks` keyset is built from a plan-building path; old code had identical behavior. Scope was `replan.ts` only → no `nodes.ts` collision with R3. Next: **R3 (scheduling edge cases; #22/#26/#15)**. Remaining after: the **OS-level-sandbox spike** + the papercuts sweep.

Update this file's "Status" + check off cycles as they merge.

---

## Execution notes for the next session (batching + sequencing)

**Eleven cycles shipped (S2, U1, P1, P2, S1, S3+S4 batched, S5, P3, HITL+R2 batched, R1 — all merged to main)** + the live-verification session (#35) DONE. Remaining:
**R3 + the NEW OS-level-sandbox spike** (from S1's residual) + the ~30-Minor papercuts sweep (now also holding R1's unreachable `frozenInOrder` note). Guidance for running them efficiently:

- **`nodes.ts` collision → keep the run-loop cluster SERIAL.** R1, R2, R3 (and the now-merged S5) all rewrite `nodes.ts` (the ~1,500-line run loop); `project-store.ts` (R3) and `run-state.ts` (R2) are shared. Do **not** parallel-branch these — they'd merge-conflict in the most interdependent file. Run R1 → R2 → R3 one at a time. (S5 done — its `nodes.ts` touch was a single-line redaction at the resume capture point, no conflict surface left for R-cycles.)
- **Papercuts note:** S5 already absorbed a pre-existing non-S5 bug (non-atomic `recents` write + a shared-`/tmp` test-isolation flake). Other latent items may surface mid-cycle the same way — fold small discovered fixes in with user OK, or log to the papercuts sweep.
- **Parallelism, if any, only via git worktrees** (never multiple terminals on one repo dir — that races `.git/index`/HEAD). The safe ceiling is ~2 file-disjoint cycles at once, e.g. **S3 (or S4)** in one worktree + **P3** (`run-store`/`orchestrator`/`ipc` — no `nodes.ts`) in another.
- **Hard sequencing — SATISFIED (live-verify done 2026-06-29).** R1 is **un-gated**: re-plan/escalation won't fire naturally, but its `mergeReplan` bugs (#6/#7/#13) are deterministic + unit-testable, so proceed via code fixes + tests. R2 is **confirmed live** (handoffs not persisted). The R-cluster + the **NEW HITL resume→synthesis bug** all touch `nodes.ts` → still run them SERIAL. The HITL bug + R2 are thematically related (a resumed/consulted sub-run's output not captured into the durable record/synthesis) — consider batching.
- **Preferred speedup = BATCH, not parallelize.** Collapse related findings into fewer, larger cycles to cut per-cycle pipeline overhead: e.g. **S3 + S4 → one "external-data trust" cycle** (both validate untrusted external input), and the **~30 Minors → one "papercuts sweep"** cycle. Fewer design rounds + fewer branches, same per-branch agent throughput.
- **Per-cycle execution defaults** (see the `ai-manager-cycle-workflow-prefs` memory): subagent-driven; auto-merge to main locally on a clean opus final review. Human gates = design + spec approval only.
